(async()=>{"use strict";const db=window.filings4uPublicSupabase,$=s=>document.querySelector(s);let posts=[];const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));const date=v=>v?new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric"}).format(new Date(v)):"";const r=await db.from("filings4u_blog").select("*").eq("status","published").order("published_at",{ascending:false});if(r.error){$("#blogGrid").innerHTML='<div class="content-loading">Articles could not be loaded.</div>';return}posts=r.data||[];const cats=[...new Set(posts.map(x=>x.category).filter(Boolean))].sort();$("#blogCategory").innerHTML='<option value="">All categories</option>'+cats.map(x=>`<option>${esc(x)}</option>`).join("");function render(){const q=$("#blogSearch").value.toLowerCase(),cat=$("#blogCategory").value;const a=posts.filter(x=>(!cat||x.category===cat)&&(!q||[x.title,x.excerpt,x.category].join(" ").toLowerCase().includes(q)));$("#blogGrid").innerHTML=a.length?a.map(x=>`<article class="blog-card" data-slug="${esc(x.slug)}"><img src="${esc(x.featured_image_url||"images/llc-formation-hero.jpg")}" alt=""><div class="blog-card__body"><div class="blog-card__meta"><span>${esc(x.category||"Insights")}</span><span>${date(x.published_at)}</span></div><h2>${esc(x.title)}</h2><p>${esc(x.excerpt||"")}</p><b>Read article →</b></div></article>`).join(""):'<div class="content-loading">No articles found.</div>';document.querySelectorAll("[data-slug]").forEach(el=>el.onclick=()=>open(el.dataset.slug))}function open(slug){const x=posts.find(p=>p.slug===slug);if(!x)return;$("#articleHero").innerHTML=`<header class="article-header"><img src="${esc(x.featured_image_url||"")}" alt=""><div class="article-header__copy"><span>${esc(x.category||"Insights")}</span><h1>${esc(x.title)}</h1><p>${esc(x.excerpt||"")}</p></div></header>`;$("#articleBody").innerHTML=x.content||"";document.querySelector(".content-hero").hidden=true;document.querySelector(".content-section").hidden=true;$("#articleView").hidden=false;history.replaceState({slug},"",`blog.html?slug=${encodeURIComponent(slug)}`);scrollTo({top:0,behavior:"smooth"})}$("#backToBlog").onclick=()=>location.href="blog.html";$("#blogSearch").oninput=render;$("#blogCategory").onchange=render;render();const slug=new URLSearchParams(location.search).get("slug");if(slug)open(slug)})();